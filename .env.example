# dsh-literature environment template.
# Copy to .env only if you use an env loader; the plugin reads process.env
# directly, so exporting the variable in your shell works too.
#
# Optional but recommended: OpenAlex API key (anonymous mode works without it).
# The key is read from the environment only — never commit a real value.
OPENALEX_API_KEY=
